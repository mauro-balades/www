
import styled from "styled-components";

export const ManPageWrapper = styled.div`
    overflow: auto;
    height: calc(100% - 20px);
    width: calc(100% - 20px);

    padding: 20px;

    & * {
        font-size: 1em;
        font-weight: bold;
        maring: 0;
    }
`
